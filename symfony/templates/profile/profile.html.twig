{% extends 'base.html.twig' %}

{% block title %}[Username] - Profil{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('styles/profile.css') }}" rel="stylesheet">
{% endblock %}

{% block content %}
    {# <div class="middle"> #}
    {# <div class="child1"> #}
    {# <div class="picture"><img src="{{ asset('images/default_picture.png') }}"></div> #}
    {# <div class="right"> #}
    {# <p class="title">[Username]</p> #}
    {# <div class="bottom"> #}
    {# <p class="role">Rôle</p> #}
    {# <button class="edit">Modifier</button> #}
    {# </div> #}
    {# </div> #}
    {# </div> #}
    {# <div class="child2"> #}
    {# <h1 class="title">Ceci est un titre</h1> #}
    {# <p>Et ça un paragraphe (époustouflant)</p> #}
    {# </div> #}
    {# </div> #}
    <h1>Create new User</h1>

    {{ form_start(form) }}
    <h3>Addresses</h3>
    <ul class="addresses"
        data-index="{{ form.addresses|length >0 ? form.addresses|last.vars.name +1 : 0}}"
        data-prototype="{{ form_row(form.addresses) }}">

    </ul>
    <button type="button" class="add_item_link" data-collection-holder-class="addresses">Add an address</button>
    {{ form_end(form) }}
{% endblock %}

